import React from 'react';
import moduleStatepublisher from '../../../components/module-state-publisher';

const dashboardPolicies = (state) => {
	return <div>
		Policies
	</div>
}

export default moduleStatepublisher(dashboardPolicies, "DashboardPolicies");