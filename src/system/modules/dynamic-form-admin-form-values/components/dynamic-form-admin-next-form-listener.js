import React from 'react';
import moduleStatepublisher from '../../../components/module-state-publisher';

const dynamicFormAdminNextFormListener = (state) => {
	return null
};

module.exports = moduleStatepublisher(dynamicFormAdminNextFormListener, "DynamicFormAdminNextFormListener");