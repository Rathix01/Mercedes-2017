import React from 'react';
import moduleStatepublisher from '../../../components/module-state-publisher';

const dynamicFormAdminUpdateListener = (state) => {
	return null
};

module.exports = moduleStatepublisher(dynamicFormAdminUpdateListener, "DynamicFormAdminUpdateListener");