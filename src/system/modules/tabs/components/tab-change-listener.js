import React from 'react';
import moduleStatepublisher from '../../../components/module-state-publisher';

const tabChangeListener = (state) => {
	return null
};

module.exports = moduleStatepublisher(tabChangeListener, "TabChangeListener");