import React from 'react';
import moduleStatepublisher from '../../../components/module-state-publisher';

const dashboardClaims = (state) => {
	return <div>
		Claims
	</div>
}

export default moduleStatepublisher(dashboardClaims, "dashboardClaims");