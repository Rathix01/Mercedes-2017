import React from 'react';
import moduleStatepublisher from '../../../components/module-state-publisher';

const dashboardLockbox = (state) => {
	return <div>
		Lockbox
	</div>
}

export default moduleStatepublisher(dashboardLockbox, "dashboardLockbox");