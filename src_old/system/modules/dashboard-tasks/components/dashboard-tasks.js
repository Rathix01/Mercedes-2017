import React from 'react';
import moduleStatepublisher from '../../../components/module-state-publisher';

const dashboardTasks = (state) => {
	return <div>
		Tasks
	</div>
}

export default moduleStatepublisher(dashboardTasks, "DashboardTasks");