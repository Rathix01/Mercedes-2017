import React from 'react';
import Read from '../../../components/read-state'

const testComponent = (state) => {
      return (
        <div>
          TEsTs
        </div>
    );
}

module.exports = Read(testComponent, "TestComponent");
